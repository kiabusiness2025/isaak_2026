# scripts/verify

Scripts de verificación post-migración: smoke tests de rutas migradas, chequeos de paridad
entre `isaak` (legacy) e `isaak_2026`, validación de que no se filtran secretos o assets no
aprobados.

Carpeta creada como parte del esqueleto de estructura — todavía sin scripts.
