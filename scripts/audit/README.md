# scripts/audit

Scripts de auditoría del repo: dependencias desactualizadas, tamaño de bundle, cobertura de
`prefers-reduced-motion`, paridad de copy contra `packages/content`, etc.

Carpeta creada como parte del esqueleto de estructura — todavía sin scripts. El repo `isaak`
no tiene un equivalente `scripts/audit/`; sus auditorías (Fase 0) se hicieron como documentos
en `docs/migration/`, no como scripts ejecutables.
