# apps/web

**Función:** web pública de Isaak en `isaak.es`.

Next.js 15 (App Router), V1 estática — sin backend, sin DB, sin auth. Ver `CLAUDE.md` en la
raíz del repo para el desglose completo de rutas, componentes y el sistema de poses del
personaje Isaak.
