# @isaak/testing

**Función:** helpers de test, fixtures, mocks — compartidos entre `apps/web`, `apps/app` y
`apps/admin`. No existe como paquete dedicado en el repo `isaak` — solo hay un `e2e/`
suelto en la raíz de ese repo, no un paquete de workspace.

Estado: esqueleto vacío, sin contenido migrado. Antes de portar código real, revisar la
ficha de migración correspondiente en `docs/migration/reusable-modules.md` (repo `isaak`).
