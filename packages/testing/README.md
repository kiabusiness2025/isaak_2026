# @isaak/testing

Utilidades y setup compartido de tests (Playwright/Vitest). No existe como paquete dedicado en el repo isaak — solo hay un e2e/ suelto en la raíz del repo, no un paquete de workspace.

Estado: esqueleto vacío, sin contenido migrado. Antes de portar código real, revisar la
ficha de migración correspondiente en `docs/migration/reusable-modules.md` (repo `isaak`).
