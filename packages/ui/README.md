# @isaak/ui

**Función:** componentes visuales compartidos entre `apps/web`, `apps/app` y `apps/admin`
(más allá de las primitivas propias de `apps/web/components/ui`, que se quedan ahí).
Equivalente en el repo `isaak`: `packages/ui` (53 archivos).

Estado: esqueleto vacío, sin contenido migrado. Antes de portar código real, revisar la
ficha de migración correspondiente en `docs/migration/reusable-modules.md` (repo `isaak`).
