# @isaak/config

Configuración compartida (ESLint, TypeScript, Prettier). En el repo isaak existen como dos paquetes separados — eslint-config y typescript-config — en vez de uno unificado; decisión de consolidar o mantener separados queda pendiente para cuando se migre contenido real.

Estado: esqueleto vacío, sin contenido migrado. Antes de portar código real, revisar la
ficha de migración correspondiente en `docs/migration/reusable-modules.md` (repo `isaak`).
