# @isaak/db

Cliente de base de datos y schema compartido (Prisma). Equivalente en el repo isaak: packages/db (129 archivos, schema + migraciones) — el paquete de mayor tamaño y el núcleo real del backend a migrar.

Estado: esqueleto vacío, sin contenido migrado. Antes de portar código real, revisar la
ficha de migración correspondiente en `docs/migration/reusable-modules.md` (repo `isaak`).
