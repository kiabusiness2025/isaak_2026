# @isaak/billing

Lógica de planes, cuotas y facturación (Stripe). No existe como paquete dedicado en el repo isaak — hoy vive disperso dentro de apps/app (isaak-quota.ts, isaak-billing-helpers.ts, isaak-feature-gate.ts) y está marcado como 'rediseñar desde cero', no extraer tal cual.

Estado: esqueleto vacío, sin contenido migrado. Antes de portar código real, revisar la
ficha de migración correspondiente en `docs/migration/reusable-modules.md` (repo `isaak`).
